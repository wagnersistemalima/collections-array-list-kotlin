package com.sistemalima.collections

fun main() {

    val values = intArrayOf(2, 3, 4, 1, 10)

    values.sort()
    values.forEach { it -> println(it) }

    println("-------------------------------")
}